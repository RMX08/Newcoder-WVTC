#include <algorithm>
#include <iostream>
using namespace std;

int main() {
    // 方法一：
    string n;
    cin >> n;
    reverse(n.begin(), n.end());
    cout << n;
    return 0;

    // 方法二：
    int a;
    cin >> a;
    cout << a%10 << a/10%10 << a/100%10 << a/1000 << endl;
    return 0;
}
